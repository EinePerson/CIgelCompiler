add variable access to local variables without usage of this
add partly libraries;
add package manager;
loop labels
add real string as type

add throwables common super class; //Not needed but nice
add class enums;

add expends for structs (like extends for classes)

memory safety:
    pointers always need size parameter to identify overflow

make compilable to emscripten

annotations for not mangle usw

add function calling with array as parameter

add multithreading support:
