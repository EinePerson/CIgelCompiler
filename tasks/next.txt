add final variables;
add interfaces;
add enums;
type casting;
add partly libraries;
add debug info;
add header parsing for include and usage of c(++) files;
add package manager;

add exceptions, try/catch;