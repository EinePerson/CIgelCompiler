type casting;
add acces modifieres
add template classes;
add partly libraries;
add debug info;
add header parsing for include and usage of c(++) files;
add package manager;

add throwables common super class; //Not needed but nice
add class enums;